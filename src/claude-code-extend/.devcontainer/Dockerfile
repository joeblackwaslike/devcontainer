FROM ghcr.io/joeblackwaslike/devcontainer:latest

# Add project-specific tools or configuration below.
# The base image already includes Python, Node, Go, Rust, Ruby, Bun, Deno,
# Claude Code, Gemini CLI, Codex CLI, uv, pnpm, PostgreSQL, Redis, and more.
#
# Examples:
#   RUN npm install -g @myorg/internal-cli
#   RUN pipx install mypackage
#   RUN GOPATH=/usr/local go install some/tool@latest
#   COPY custom-config/ /etc/myapp/
